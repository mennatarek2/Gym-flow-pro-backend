# REMEDIATION PLAN — GymFlowPro / GMS

Controlled hardening pass. Baseline: Phase 1 discovery + PROJECT-VALIDATION-REPORT.md.
Principle: smallest safe fix, evidence-first, tests with every change, existing user working-tree changes preserved untouched.

Working-tree note (pre-existing, NOT ours — preserved): 14 modified files around Inventory/Purchasing + .gitignore. None of our fixes touch those files except .gitignore (appended only) and tests we add in NEW files.

| ID | Finding | Severity | Evidence | Planned Fix | Risk | Tests |
|----|---------|----------|----------|-------------|------|-------|
| F1 | Background jobs run without ITenantContext → global query filters inactive; jobs rely on per-query explicit TenantId. Sampled jobs are safe (explicit TenantId / raw SQL w/ tenant param), but no fail-closed guarantee exists. | HIGH | GymFlowProDbContext (filters null-guarded); AnalyticsAggregationJob pattern; JobScheduler | Audit all job DbContext resolutions; add `EnsureNoAmbientTenantFilterReliance` guard documentation + a regression test proving filtered-out behavior; document each IgnoreQueryFilters cluster. No runtime redesign — filters already inactive by design in jobs and every job sampled constrains by TenantId explicitly. | Low (docs+tests) | New test: query without tenant context does not return other tenants' rows when TenantId predicate present; job-style aggregation test |
| F2 | Hardcoded AES fallback key `GymFlowPro-AES256-DefaultKey-32C` silently used when EncryptionKey missing | HIGH | AesEncryptionService ctor | Fail fast in Production/Staging when EncryptionKey missing; dev-only fallback allowed behind environment check | Low | New unit tests: prod-missing-key throws; dev fallback works |
| F3 | Hardcoded ngrok URL default in apps/web api-client.js | MEDIUM | api-client.js:22 | Remove ngrok default; use window.API_BASE/localStorage override; empty default → same-origin `/api` (matches production static-serving architecture) | Low | Manual verify + grep no ngrok in src |
| F4 | Unauthenticated OTP/activation endpoints lack rate limiting | MEDIUM | AuthController member-otp/member-verify/member-activate; OtpCacheService | Add lightweight in-memory per-phone+IP throttle on OTP send/verify (no new deps). Activation code is single-use hashed+peppered — adequate; document. | Low-Med | Unit tests for throttle behavior |
| F5 | Cash movement recording post-commit best-effort (assign/renew/sale/record-payment): drawer can drift from PaymentTransactions | MEDIUM | PersistPaidMembershipAsync; CreateSaleAsync post-commit block; RecordPaymentAsync | Smallest safe fix: retry transient failures once inside RecordMovementAsync callers? NO — instead make RecordMovementAsync itself retry transient DB errors internally (3 attempts), preserving post-commit semantics. Full transactional coupling would change established behavior — deferred to report as residual risk with recommendation. | Low | Existing shift/membership desk-outstanding tests still pass; new retry unit test if feasible without new infra |
| F6 | Desk assign/renew lack idempotency; racing assigns possible (no unique constraint) | MEDIUM | MembershipService.Assign/Renew | Do NOT add migration/schema changes (out of scope for controlled pass without user approval of migrations). Document; strengthen in-code race window minimally? Deferred — documented only. | n/a | n/a (documented) |
| F7 | Attendance write vs session decrement non-atomic; 0-row decrement tolerated | MEDIUM | CheckinService.DecrementSessionsIfNeededAsync | Minimal fix: check affected-rows of the guarded UPDATE; if 0 for a session-pack membership that validated >0, return failure response instead of success (attendance already written — do NOT delete attendance; keep rule #3 "post-attendance"). This converts silent drift into an explicit error surfaced to staff. | Low | Adjust/add CheckinServiceTests case |
| F8 | WeatherForecastController dead template code | LOW | GMS.Api/Controllers/WeatherForecastController.cs | Verify zero references incl. tests/swagger, then delete file | Very low | Build + full tests |
| F9 | Repo hygiene: ~700MB archives, publish*/ dirs, .vs tracked | MEDIUM | git ls-files | Update .gitignore; `git rm --cached` artifacts only (no history rewrite; no working-file deletion of unknown archives) | Low | git status clean-ish after |
| F10 | No CI | P1 | empty .github/workflows | Add build+test workflow using actual commands (`dotnet build GMS.slnx`, `dotnet test GMS.Tests/GMS.Tests.csproj`) | Low | Workflow YAML lint via actionlint-equivalent (manual) |
| F11 | Docs claim Flutter app / features beyond repo reality | LOW | docs/, Frontend/*.md | Add STALE/STATUS banner headers to key mismatch docs; no rewrites | Very low | n/a |
| F12 | PermissionsOverride read-but-unused | DOC | DefaultPermissionProvider remarks | Document decision in report; no speculative implementation | n/a | n/a |

Explicitly NOT done (per instructions): SaleService decomposition (P2), stringly-typed statuses (P2), schema/migrations (F6), Paymob/Fawry runtime verification (unknown), wwwroot rebuild.
