# Visual Studio
.vs/
*.user
*.suo

# Build output
bin/
obj/

# Publish credentials (passwords)
*.pubxml.user

# Environment / secrets
.env
.env.*
!.env.example
!.env.*.example
*.local.json
appsettings.*.local.json
# NOT a personal secrets override (those are lowercase "*.local.json" above) — this is the
# committed, Development/Production-style environment config for the Local Edition deployment.
!appsettings.Local.json

# Logs
*.log
logs/

# Temp / scratch
tmp/
*.tmp
*~
tmp-*.txt

# Local uploads (staff photos, PDFs — do not commit)
GMS.Api/wwwroot/uploads/

# Nested Frontend repo — keep separate until intentional monorepo migration
Frontend/
Frontend.zip
Frontend.rar
Frontend*.zip
Frontend*.rar

# Local publish output (contains deployed binaries / config copies)
publish/
publish-test/
publish-verify/
publish-local/
publish-local-phase3/
scripts/local-install/publish-output/
scripts/local-install/launcher-output/
scripts/local-install/Output/

# Tooling / local agent state
.claude/
.wolf/

# OS
.DS_Store
Thumbs.db

publish-local-recovery/
publish-local-verify/
publish-usb-latest/
artifacts/
publish-dist/
HyMotion-Desktop-Release.zip

